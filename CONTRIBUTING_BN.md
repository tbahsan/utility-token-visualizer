# অবদান নির্দেশিকা (CONTRIBUTING)

## কোড না জেনেও যেভাবে সাহায্য করবেন

1. **শুল্ক হার আপডেট:** `tariffs/tariffs.json` খুলে `slabs` রেট বদলে দিন, `last_verified` তারিখ দিন, Pull Request পাঠান। রসিদ/গেজেটের ছবি PR-এ যুক্ত করুন।
2. **ভাষা:** `i18n/bn.json` ও `i18n/en.json`-এ একই key-তে দুই ভাষার মান থাকতে হবে।
3. **যন্ত্রপাতি:** `data/appliances.json`-এ ওয়াট/ঘণ্টা সংশোধন করুন।
4. **বাগ:** Issues → `bug` টেমপ্লেটে ধাপ + স্ক্রিনশট দিন।

## নিয়ম

- বাংলা স্ট্রিং আগে (`bn.json`), তারপর ইংরেজি — দুটোতেই key মিলতে হবে (CI যাচাই করে)।
- নতুন ফিচারে `data-i18n` key + উভয় JSON আপডেট বাধ্যতামূলক।
- কোনো থার্ড-পার্টি ট্র্যাকার/CDN-বাধ্যতামূলক লাইব্রেরি নয় — কোর ফিচার CDN ছাড়াই চলতে হবে।
- টাকা-পয়সার হিসাবে পূর্ণ সংখ্যা-পয়সা বা `r2()` রাউন্ডিং ব্যবহার করুন; সূত্র UI-তে/কমেন্টে লিখুন।

ধন্যবাদ! 🙏 (ইমোজি শুধু এখানে — অ্যাপে নয় 😄)
